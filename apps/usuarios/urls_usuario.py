from django.conf.urls import url
from django.contrib.auth.decorators import login_required


# from .views import PerfilUsuario

urlpatterns = [


    # url(r'^perfil/(?P<pk>\d+)/$', login_required(PerfilUsuario.as_view()), name='perfil-usuario'),
]
